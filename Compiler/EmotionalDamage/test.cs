int kage = 2;
float kage = 6.9;
if (kage == kage) {
    println(kage);
    println("Kage");
}
elif(kage == 2) {
    int kage = 2 * 6 + 3 / 4;
    float kage = sqrt(2 ** 5);
}
elif (kage == 2) {
    while (kage == 2) {
        print("kage");
    }
}
else {
    for (int i = 0; i < 10; i++) {
        print(i);
    }
}

int kage(int x, int y) {
    int z = x + y;
    return z;
}

if (kage > 1) {
    int[5] m;
    int[5][5] m2;
    if (kage != 0) {
        for (int i = 0; i < m2.row; i++) {
            for (int j = 0; j < m2.col; j++) {
                m2[i][j] = 5;
            }
        }
    }
}

bool t = true;
